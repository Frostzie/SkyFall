package io.github.frostzie.nodex.domain.project

/**
 * Represents the type of project.
 */
enum class ProjectType {
    GENERIC,
    DATAPACK,
    RESOURCEPACK,
    BOTH
}

/**
 * Represents the specific type of pack.
 */
enum class PackType {
    DATAPACK,
    RESOURCEPACK
}