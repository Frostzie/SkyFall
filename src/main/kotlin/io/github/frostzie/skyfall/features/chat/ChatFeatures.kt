package io.github.frostzie.skyfall.features.chat

import io.github.frostzie.skyfall.SkyFall
import io.github.frostzie.skyfall.utils.ChatUtils
import net.fabricmc.fabric.api.client.message.v1.ClientReceiveMessageEvents
/*
class ChatFeatures {

    fun onChatReceive(event: ClientReceiveMessageEvents.Chat) {
        val message = event.toString()
        if (SkyFall.config.chat.ChatTest) {
            ChatUtils.messageToChat(message)
        }
    }
}

 */